#include "lib/sys.h"

/*

argv[0] = install
argc[1] = operation (mod,diskln)

*/

int main(int argc, char *argv[]) {
    /*
    char **args = argv;
    int argcount = argc;
    if (argc < 2) {
        sys_write(STDOUT, "Too few arguments!\n", 19);
        sys_exit(1);
    }
    char *source = argc[1];
    */
    sys_exit(0);
}